# nlm-memory CHANGELOG

Session-level log per session protocol. Cap: 10 entries — archive older to `CHANGELOG-YYYY.md` when exceeded.

## 2026-09-17 - Exemplar embed-backfill wired into the daemon's maintenance loop; closes the #457 "dead recall_code" follow-up

**Changes:** Investigated NLM #457's carried-over "1,541 code_exemplars vs 2 vector chunks" figure — stale. Direct query against `code_exemplars` / `code_exemplars_vec` showed 1468/1542 already vectored; the real gap was 74 rows. Ran the existing operator CLI (`nlm embed-backfill --exemplars --verbose`): 74/74 succeeded, full 1542/1542 parity, `recall_code` verified end-to-end returning real matches. Wired the same repair into the daemon's existing in-process maintenance timer lineup in `src/cli/nlm.ts` (same `setInterval` pattern as the corpus monitor / wiki projection / signal prune) — SQLite-only, gated on `NLM_CODE_EXEMPLARS_ENABLED=1`, 24h cadence, 2-min startup delay — so a future dropped-embed race self-heals instead of silently degrading `recall_code` again.

**Decisions:** Cherry-picked the single new commit onto a fresh branch off `origin/main` rather than rebasing the stale `fix/recall-outcome-observability` branch forward — two of its three unpushed commits were already on `main` under different SHAs (one patch-identical, one genuinely diverged because `reports/re-derivation/decision-vectors.jsonl`, a large generated report, differs per-environment). Cherry-pick avoided replaying that divergence as a false conflict.

**State:** `origin/main` at `198e69f` (pushed via fast-forward, no PR gate). Typecheck clean; exemplar-backfill + scheduler-exemplars integration tests passing on top of current main. Left ~577 lines of pre-existing, unrelated plugin-bundle build drift (`nlm/index.js`, `plugin/scripts/*.mjs`) untouched — not this session's to fix, logged as a Backlog task instead.

**Next:** Rebuild + commit fresh plugin bundles (nlm-memory Tasks, P3). No other #457 follow-ups touched this session — FTS bm25 cold recall (~3.4s) and VACUUM/ANALYZE scheduling remain open.

## 2026-08-25 - The 0.21.5 "embedder regression" does not exist; source attribution, test isolation, warmup retry

**Corrections first, because three open diagnoses were wrong and the wrong ones cost the most time.**

**0.21.5 does NOT regress the prose embedder (board #450, three weeks P1).** Reproduced the reported signature with zero code changes. Ran the actual staged `versions/0.21.5` payload against a copy of the real 1GB corpus under the same bundled node: `ready=True` in 25s. Then the same payload, unmodified, with only the prose model name pointed at something LM Studio will not serve: `ready=False, textEmbedder=False, prose='unknown', code='ok'` — that row's reported signature exactly, code lane included. What happened on 08-14 was the prose model not being servable at boot; LM Studio loads on demand, so the code model can be resident while nomic-v1.5 is not, which is why the code lane looked like it exonerated the backend. The static-JSON-import hypothesis was independently weak: `src/http/app.ts` on healthy 0.21.6 already does that exact import in the daemon's early module graph.

**The real defect was the warmup latch, and it manufactured that false root cause.** `embedder.embed("warmup init").then(markWarm).catch(() => {})` — one attempt, silent catch, no retry. A transient backend absence latched the stage cold for the daemon's entire uptime, so restarting inside the same outage window reproduces it every time and reads as determinism, while a later rollback appears to fix it. Fixed: `retryUntilWarm` retries on capped exponential backoff, and failures now record reason and attempt count so health reports `LLM unreachable: openai-embedder, attempts 4` instead of a bare false. Verified end to end: booted with the embedder absent, brought it up underneath the same pid, recovered with no restart.

**~800 recall probes/day were self-inflicted (board #437).** Two emitters, split by `limit`. 12,961 rows at `limit=1` came from `nxtoskit/verify/orchestrate.py`, which ran a full recall query to test reachability once per verification run — not on a schedule, which is why volume swung 12/day to 2670/day and looked like a rogue prober. Now uses `/api/health`. The other 2,169 at `limit=20` were this suite: `vitest.config.ts` never set `NLM_STATE_ROOT`, so any test touching `/api/recall` appended to the operator's live `query_log.jsonl`. One run of `tests/integration/http.test.ts` wrote 5 rows. Now pinned to a mkdtemp root; full suite writes zero.

**Session-start recalls were logged as prompt-hook traffic since `b69db02`.** That dedup replaced the hook's own `recallOverHttp`, which sent `x-recall-source: session-start-hook`, with the shared helper that hardcodes `"hook"`. The query log dies on exactly that schedule: 670 rows in June, 31 in July, 0 in August, while the hook kept firing hundreds of times a month. Per-source precision cannot separate two paths sharing a label, and their ceilings are not comparable — prompt recall queries a real user prompt, session-start queries a derived project name. `source` now defaults to `"hook"`, so only the session-start call site opts into its own bucket.

**Also corrected, not fixed:** board #419's stated hypothesis is wrong. 7 of 8 temporal queries in the locked gold set carry a named entity, so the force-include gate already fires on them; all three hybrid misses are gate-fires and the single gate fall-through is a hit. The real mechanism is that `forceIncludeKeywordTop` rescues only `kwHits[0]`, and every miss has `kw_R@1 = 0`, so it inserts a non-gold session and can evict the gold one it exists to protect.

**State:** `origin/main` at `f9eff91`, 14 commits pushed. The 0.21.6 release line was replayed onto main with `f7e6ca3`'s corpus artifacts dropped, so main now matches what the install actually runs; one real conflict in `hook-log.ts` where both sides add a distinct optional field (`v?` and `recallOutcome?`), resolved by keeping both. Version bump commits deliberately skipped — main carries work 0.21.6 lacks, so numbering is an operator call. Suite 2837 passing, typecheck clean. `reports/re-derivation/` bulk artifacts (671 judge-cache files, decision-vectors, labels, sample, spot-check) were verified absent from published history and stay unpushed; the 4 curated Stage A files that `e35107b` deliberately published are untouched.

**Next:** #455, a Hermes `state.db` adapter. Hermes migrated from JSON files to per-profile SQLite around 2026-05-21, so the file adapter has captured nothing since: 25 profiles, 1,091 sessions, 113,351 messages the corpus has never seen. The plugin cannot cover it either — gate run before rolling it out, and none of its three hook routes writes a session row. Also #456: NxtCron records run history only for the Pro, so 55 jobs on the ssh-transport hosts are inventory-only and a job that stops firing raises nothing.

## 2026-08-14c - Hook version parity built and green, NOT deployed: 0.21.5 regresses the prose embedder

**Changes:** Built the parity check that closes the gap the liveness canary structurally can't cover. `hook-liveness` answers "did hooks fire?"; it cannot answer "did the RIGHT hooks fire?", which is why nine days of 0.21.0 hook code ran against a 0.21.3 daemon with every check green. Hooks now stamp their own version into each hook-log line at both writers (`appendHookLog`, `appendHookEvent` — the two choke points, so every hook is covered), and `checkHookVersionParity` compares the most recent LIVE fire against the installed version, alerting on skew or on a fire carrying no version at all (definitionally a pre-stamp build). Wired into `cli/digest.ts` beside the existing two canaries. 9 tests, full suite 2576 green, typecheck clean. Committed `5817885`, released as 0.21.5.

**Decisions:** Parity reads only the newest live fire rather than scanning a window. A window still holds pre-upgrade entries, so it would alert for a day after every legitimate upgrade — and a canary that cries wolf on routine upgrades gets muted, which is precisely how the original drift survived undetected. Newest-fire self-clears one hook run after an upgrade while a genuinely stale hook reports the old version forever. Bumped to 0.21.5 rather than restaging 0.21.4 with different content: a check about version integrity must not ship under a lying version.

**State — NOT DEPLOYED, and this is the important part:** 0.21.5 breaks the prose text embedder. Reproduced twice on clean restarts: `warmup.textEmbedder` stays false past 150s and `embedding.prose` reports `unknown` (the code lane stays `ok`), so the daemon never reaches ready. Rolling `current` back to 0.21.4 restored it in 10 seconds — causality is unambiguous. Ruled out by direct comparison: node_modules listings byte-identical (165 pkgs, `@huggingface/transformers` 4.2.0 in both), `assets/` identical, dist structure identical, `dist/shared/version.js` imports fine under the bundled node, nothing in `daemon-err.log`. The only delta is the 0.21.5 code itself. **Live right now: 0.21.4, healthy, textEmbedder true, ready true.** Hooks follow `current` so they're on 0.21.4 as well, which carries the `isMainModule` fix, so capture is intact. The parity canary is written and green but NOT protecting anything yet.

**Next:** NocoDB #450. Best untested hypothesis is the static `import ... with { type: "json" }` now sitting in two modules the daemon loads early, changing ESM evaluation order for the transformers warmup downstream (`cli/nlm.ts` does the same import, but as an entrypoint, not inside the daemon's early graph). Cheapest first move is to drop the static JSON import and read the version lazily at call time or bake it at build, keeping it out of the module graph. If that doesn't do it, bisect `5817885` — stamp-only vs parity-module-only isolates it in two builds.

## 2026-08-14b - Install unification: one NLM on the machine, and the symlink bug that was hiding behind it

**Changes:** (1) Merged `fix/hook-env-lazy-reads-and-http-mcp-default` (4 commits, unmerged since 08-05) into main — including `edc7cad`, the fix for the module-scope env reads that had passive recall dead 07-27 to 08-05. Those commits were live in production only because the five Claude Code hooks pointed into that working checkout's `dist/`, so a feature branch in a working directory was the de facto hook deployment. (2) Released 0.21.4; bumping `plugin/.codex-plugin/plugin.json` (which the release process has never touched — it sat at 0.21.0 through three releases) turned the suite fully green for the first time, 2567 passing. (3) **Found and fixed a silent hook-disabling bug**: all five hooks guarded `main()` with `import.meta.url === pathToFileURL(process.argv[1]).href`. Node realpath-resolves `import.meta.url` but leaves `argv[1]` literal, so through ANY symlinked path the two disagree, the guard goes false, `main()` never runs, and the hook exits 0 having done nothing — no output, no log, no error. Extracted to `isMainModule(metaUrl, argv1)`, which realpaths `argv[1]` first; covered by a test building a real symlinked dir. (4) Installed 0.21.4 into the NxtOS versioned tree, flipped `current`, repointed all five hooks at `current`, replaced the `npm link`ed `nlm` on PATH with a shim that always follows `current`. (5) Wired `embed-backfill --facts` into the 07:00 digest (Whtnxt Agent `5bc9e42`), plus an earlier fix (`f77e1e7`) pointing the integrity sweep at the deployed CLI.

**Decisions:** Hooks follow `current`, not a pinned version dir — reversing an earlier lean. Pinning is precisely what caused this: a fixed path that never follows releases, drifting silently. `current` makes hooks and daemon the same code by construction, and the counter-risk (a bad release breaks capture) is loud and observable in `hook-log.jsonl` rather than silent. Did NOT publish 0.21.4 to npm: the registry has been irrelevant to this machine for three releases (npm latest 0.21.0 while the daemon ran 0.21.3), `nlm upgrade` would therefore *downgrade*, and publishing means scrubbing 50 unpushed commits on a public repo — a separate deliverable. Kept the backfill out of the integrity sweep: that script's JSON contract spans 9 `emit_and_exit` call sites and is about violation detection, not repair.

**State:** main at `f920c06`+; worktree `.claude/worktrees/fact-embed-backfill`. NOT pushed (main now ~54 commits ahead of origin, public repo, unscrubbed). Verified end to end: daemon reports 0.21.4 healthy with warmup ready; all five hooks execute verbatim from `settings.json` and write to `hook-log.jsonl` (prompt-recall correctly silent — `NLM_HOOK_PROMPT_RECALL=off`, confirmed identical to the old hook and proven capable by forcing it on); `which nlm` → shim → 0.21.4 with `--facts`; sweep dry-run reports `I5a=31` matching `runChecksOnSqlite`; backfill proven on a seeded copy (3 deleted, 3 found, 3 repaired, 0 remaining). One near-miss worth recording: the first repoint broke session capture outright, caught within minutes by testing the positive property (does a marker actually land in `hook-log.jsonl`) rather than trusting exit 0 — reverted from backup, root-caused, fixed, re-applied.

**Next:** #446 fact-resurrection detector still open. The `nlm upgrade` command is a live footgun — it pulls npm latest (0.21.0) and would downgrade the daemon; make it NxtOS-aware or refuse when an NxtOS install is present. Publishing to npm remains a deliberate, separate decision.

## 2026-08-14 - Fact-embed rot recovery: `embed-backfill --facts` drains the 683-fact backlog to zero

**Changes:** Built the recovery half of #435 (the visibility half shipped 08-12 as 0.21.2). `src/core/facts/embed-backfill.ts` + `nlm embed-backfill --facts`: discovers active facts absent from `fact_embeddings`, embeds them with text byte-identical to `SqliteSessionStore.embedFacts` (`${subject} ${predicate} ${value}`.trim(), role `document`), and upserts through the store's tenant-checked `upsertEmbedding`. Mirrors `backfillExemplarEmbeddings` including its single retry on `LLMUnreachableError` — which is the point, since the measured failure is sporadic (~1 in 26 calls, a steady 3-10% per busy day since April), not a sustained outage, so most rows succeed on attempt two. Only recall-eligible facts are embedded; embedding superseded/retired rows would reintroduce exactly the ghost vectors `repair-fact-embeddings.mjs` deletes (#351). 6 integration tests, TDD, including a live-path text-shape parity assertion so the two compose functions can't silently diverge.

**Decisions:** Discovery is by absence from the vec table rather than a persisted worklist, so the command is idempotent by construction and a second run is a no-op — same choice the exemplar backfill made, for the same reason. Did not extend `reembedCorpus`'s existing fact re-embed instead: that path only fires on an embedder config change (a same-config rerun reembeds sessions only), so reaching it would have meant either faking a config change or restructuring a working command; a sibling command is smaller and independently testable. Left the pre-existing `codex-plugin.test.ts` failure alone (plugin.json pinned at 0.21.0 vs package.json 0.21.3) to keep this single-concern.

**State:** Committed `4c66cad` on `fix/fact-embed-backfill`, worktree `.claude/worktrees/fact-embed-backfill`, branched off `main` — NOT merged, NOT pushed. Typecheck clean; non-PG suite 2532 passed / 1 failed, that one being the pre-existing plugin-version drift present on `main` before this branch. Verified against a `.backup` copy of the live store first (683 -> 0, second run a no-op, zero ghosts), then run on the live store: **683 -> 0 unembedded active facts**, 678 in 13.7s, invariant checker byte-identical before vs after (`I5a:31 I5a-mv:8 I7b-1:58101`, all pre-existing). Pre-run backup at `~/.nlm/backups/canonical-20260814-094920.sqlite`. End-to-end confirmed through MCP: `fact_b289f039` (`.mcp.json owner Edward`), which had no vector an hour earlier, now returns as the #1 semantic hit at 0.8999 with `matchedIn: ["semantic"]`.

**Next:** Merge + release so the command is re-runnable from the installed CLI rather than only from this worktree — the backlog is drained but the bleed continues (656 on 08-12 -> 683 on 08-14, ~13/day) until a scheduled run exists. Then decide whether the sweep should call it nightly. Root cause is still unfixed and deliberately so: `embedFacts` swallowing per-fact failures is correct behavior for ingest (one bad embed must not abort a batch); this makes the consequence repairable instead of permanent.

## 2026-08-13 - Classifier prompt fix corrects the 08-12 muse-glimmer verdict; model promoted; runtime swap unblocked in the UI

**Changes:** (1) Root-caused the 08-12 eval's "Gemma stays" call: 4 of the 20 gold fixtures are trivial single-turn lookups (version check, `package.json` dump) where the reference expects `entities: []`, but both classifiers were extracting incidental proper nouns from command output regardless — muse-glimmer paid for it worse (its granular extraction style also surfaced file paths and hostnames the reference never listed), which is what produced 08-12's entity-F1 69.3% vs 42.6% gap. Fixed `CLASSIFIER_SYSTEM_PROMPT` (`prompt.ts`): hoisted a single "Session triage: WORK or LOOKUP" instruction above the field list (previously the trivial-session rule was only half-stated, separately, inside `confidence`'s description, with no shared definition across `entities`/`decisions`/`facts`), and added an entity-stability rule (file paths, hostnames, URLs, SHAs are never entities — redirect into `facts[]` under the matching predicate instead of dropping them). (2) Re-ran both models on the fixed prompt, n=20 each: Gemma entity-F1 69.4%->80.7%, decision-F1 46.5%->62.7%, calibration 80%->100%, p50 latency 29.8s->19.1s. muse-glimmer entity-F1 44.4%(a first run poisoned by a model-swap race right after the Gemma baseline, LM Studio hadn't settled — 14/20 fixtures came back empty at a 25ms p50, not a real timeout)/83.5%(clean re-run)->82.9%, label-accuracy 95% throughout, calibration ->100%. Entity-F1 and decision-F1 are now a wash between models (within noise at n=20); the deciding signal is label-accuracy, 70% (Gemma) vs 95% (muse-glimmer) — a ~5-fixture gap, not noise. (3) Promoted `NLM_CLASSIFIER_MODEL` to `meta/muse-glimmer` in `~/.nlm/.env` (backup `.env.bak-20260813-muse-glimmer-promotion`), daemon restarted. (4) `POST /api/classifier` hardcoded provider to `deepseek`/`ollama` only and explicitly rejected `openai` ("set via NLM_CLASSIFIER env + restart") — dead code path for the Settings > Classifier page's existing switch-model UI (provider/model dropdowns, required connection test, save), since the daemon has run classifier provider `openai` (LM Studio) since 2026-06-24. Added `ClassifierBox.openAiSwapAvailable` (true once a baseUrl was configured at daemon startup — the swap is model selection only, never endpoint selection) and let the route accept both `openai` and the Providers-registry's `openai-compatible` kind (both ride the same `DeepSeekClient` construction path). Fixed the Classifier page's active-provider matching (pre-select + dirty check) to treat both kinds as equivalent to a live `openai` classifier.

**Decisions:** Model choice was made on label-accuracy, not entity-F1/decision-F1 — those two were within eval noise at n=20 post-fix, and label-accuracy had the only gap large enough to trust at this sample size (design review via a second-opinion pass: hoist triviality to session level rather than duplicate it per-field, one contrastive example pair per distinct entity failure mode rather than matching `decisions`' 4+3 density — more examples at n=20 fixtures risks overfitting the prompt to this specific gold set, not generalizing the rule). Did not chase Gemma's pre-fix 80% calibration number — 4/20 fixtures is noise, and it corrected to 100% as a side effect of the triage hoist anyway.

**State:** `main` locally fast-forwarded through two commits (`fix(classifier): hoist session-triage rule...`, `feat(classifier): allow runtime model swap...`), full suite green modulo 4 pre-existing failures unrelated to either change (3 missing-`dist/ui`-assets, 1 `plugin.json` version drift already present on `main` before this session). NOT pushed: `main` was already 6 commits ahead of `origin/main` "by choice, public repo, not scrubbed" per the 08-12 entry below — now 48 commits ahead across that unpushed span. One own-commit scrub caught and fixed before landing: a test fixture hardcoded the real Mac Studio LAN IP, swapped for a placeholder hostname.

**Next:** decision-F1 sitting at ~50-60% for both models (flagged in second-opinion review) is the larger unaddressed product weakness this session's prompt fix didn't touch. Grow the classifier gold fixture set past 20 before tuning the prompt further — both this session's swings and 08-12's wrong verdict came from noise at the current sample size. The 08-12 entry below still applies for its other findings (I5a/#434, #435 fact-embed rot, unpushed backlog) — this entry only supersedes its classifier-eval verdict.

## 2026-08-12 - classifier gate for build-artifact facts, integrity self-heal shipped, embed rot found

**Changes:** (1) Model eval: swapped gemma-4-26b for meta/muse-glimmer and qwen3.6-35b-a3b on the same 20 fixtures via `nlm eval --classifier`. Gemma stays. Muse wins label accuracy (90% vs 75%) and calibration (100% vs 80%) but loses badly on extraction - entity F1 69.3% vs 42.6%, decision F1 61.8% vs 48.9% - at 2x the latency (p50 47.5s vs 23.7s). qwen3.6-35b is strictly dominated by gemma except a 10pt calibration edge. Extraction is the half `recall_facts` is built on, so the trade points the wrong way. Context length is a non-factor either way: transcripts are hard-truncated to 15K chars at `prompt.ts:133`, so 262k vs 131k is ~30x more headroom than gets used. (2) 0.21.1: `isEphemeralSubject` added to `coerceFacts`, sibling of `isNonAnswerValue` (#325), same call site. Blocks per-run build artifacts (`tsc|status = clean`, `task-4-review|status = Approved`) from becoming durable facts. The store carried 56 I5a violations, ~16 groups of exactly this shape. (3) 0.21.2: merged `feat/ingest-embed-failure-visibility` - fact and chunk embed failures were swallowed by bare `catch{}` in the SQLite store, a silent regression against Postgres which already logged them. (4) Store cleanup: I5a 56 -> 29, I7 3 -> 0 and holding. 284 build-artifact facts retired, 229 orphaned embeddings reaped, 12 verified-stale facts superseded, ~944MB of superseded backups reclaimed. (5) Shipped a nightly integrity self-heal in the Whtnxt Agent repo (`scripts/nlm-integrity-sweep.sh`, called from the 07:00 digest).

**Decisions:** Did NOT exempt `status` from I5a, reversing a recommendation made twice this session. The code comment at `check-invariants.ts:37` warns to add predicates only when parallelism is a domain invariant, not when the classifier failed to supersede - and it is right. `stack` is exempt because TypeScript AND SQLite are simultaneously true; `kalshi|status = 2 days stale` vs `borderline fresh` were true at different times. Exempting it would have hidden an active bug behind a green check. Also deliberately made the sweep's retroactive subject list NARROWER than the ingest gate, overriding the plan's own "one source of truth" constraint: an ingest filter false-positive merely declines to write a row, but retroactively the same list deletes accumulated knowledge, and every future addition to the gate would silently become delete authority over the whole store.

**State:** The retroactive-list flaw was not caught in design - it destroyed real facts before the final review found it. 74 judgment-bearing facts were restored (`code-quality|description = empty-guard defaults to permissive bug pattern`, `task-10-cutover|decided-on`, `task-10|blocker`), excluding by id the 19 that were deliberate retirements. 204 status/version/commit-SHA rows stayed retired. Second Critical in the same review: retirements ran one sqlite3 process per fact with no busy timeout and unchecked exit status, then rewrote the alert baseline unconditionally - under the write contention this repo already documents from 07-21, a partial sweep would have reported full success and permanently silenced whatever it failed to fix. Now one transaction with `busy_timeout`, verified applied-count, and no baseline write on failure.

**Next priorities:** #434 (P1) is the root cause and is still live - repeat ingests of the same (subject, predicate) are not superseding despite the #301 collapse logic; three cases in the last 14 days, same tenant, days apart, and I5a ticked 29 -> 31 during this session. Until it is fixed the nightly sweep is mopping with the tap on. #435 (P1): 656 active facts (3.8%) carry no embedding, a near-constant 3-10% per-day failure rate since April with no content correlation; 0.21.2 makes new failures visible but there is no fact re-embed path at all (`embed-backfill` does sessions, not facts), so the backlog will not drain. #433: calibrate the sweep's >=5 alert threshold against a week of real deltas. Unpushed: `main` is 6 commits ahead of `origin/main` by choice - public repo, not scrubbed.

## 2026-08-05 - #427 denominator fix landed, #416 corrected from delete to merge, reprocess still Studio-limited

**Changes:** (1) #427 landed and committed (765509c): `computeReDerivationRate` no longer counts a pair as eligible before checking both sides have decisions. Measured on the live corpus: eligible denominator 1,905,497 -> 635,914. Added the session-level numbers the trendable metric needs (`decisionBearingSessions`, `reDerivingSessions`, `sessionRate`) - measured 0.391% (12 re-deriving / 3,069 decision-bearing). Deliberately did NOT retire the pair-space rate, backfill the trend file, or surface anything in the digest: all three depend on the numerator, and Stage A's detector scores 6.7% precision, so publishing a rate now would put a number in front of Edward built on a detector we know is mostly wrong. (2) #416 diagnosed, and the diagnosis reversed itself mid-session in a way worth keeping. First pass: "257 twins, 250 byte-identical, safe to delete the truncated side." That was wrong - checked markers, not just bodies, and 59 of the 250 have MORE markers on the truncated side (the two rows were classified independently across the 07-27 qwen-to-Gemma promotion, so identical body does not mean identical extraction). Corrected task to a merge, not a delete: keep the full-id row, union in any markers the truncated side has that the survivor lacks, then delete. Also found the #416 framing conflated two things - only 430 of the 835 "truncated ids" are genuinely truncated claude-code uuids; 405 are a legitimate Hermes-native short-id scheme that was never broken. Real duplicate CONTENT is 104 clusters / 110 redundant rows, worst offender 5 rows for one PolySignal session. Confirmed this contaminated Stage A: the "Creating a Systems Engineer Hermes profile" GENUINE pair in the 08-04 calibration is 2 of 7 rows for one piece of work, 4 sharing an identical timestamp - the judge was labelling an ingest artifact. (3) #390 reprocess relaunched and ran to completion (462/462 processed) but only 61 succeeded, 401 failed with the known mislabeled "LLM unreachable: deepseek" error (the openai/Gemma-compatible endpoint went unreachable, not a DeepSeek call) - same Studio-churn failure mode as the 08-03 attempt. meanConfidenceNew on the 61 that landed: 0.955.

**Decisions:** #416 downgraded from "delete the twins" to "merge the twins" before any write touched the corpus - the correction happened during diagnosis, before Edward's go-ahead, so nothing was lost. Held the 08-04 push decision open rather than pushing: the calibration report names internal tool/host references (Hermes, NocoDB, NxtSites) and that call belongs to Edward.

**State:** Discovered mid-session that a second Claude Code session is working the passive-recall-hook canary in this SAME shared working directory (one `.git`, one checkout) and had switched HEAD to `fix/hook-env-lazy-reads-and-http-mcp-default` out from under this session's checkout, mid-edit on their own CHANGELOG update. `main` was unaffected (still at 765509c, confirmed via reflog) but a plain `git checkout main` would have discarded their in-progress uncommitted work - git's own safety check caught it before anything was lost. Used an isolated `git worktree` for this entry instead of touching the shared checkout again. Their session independently root-caused the actual digest warning (module-scope env reads captured before `autoloadEnv()`, plus host memory pressure - see their 08-05 entry) and, separately, made the identical public-repo-hygiene call this session made a day earlier: found unpushed local commits carrying non-public references and did not push them.

**Next:** 401-session reprocess remainder needs a real quiet-Studio window, not just an off-peak launch time - two attempts now have died the same way. #416 merge needs Edward's go-ahead before any corpus write. Two live sessions in one working tree is a standing hazard until one of us moves to an isolated worktree for the remainder of this work.

## 2026-08-04 (session 2) - Stage A built and run: the detector scores 6.7% precision, and no configuration supports an interrupt

**Changes:** (1) Ground-truth check BEFORE designing the sample overturned the parent spec's premise. 12 of the detector's 15 positives are subagent code reviews whose entire decision content is "Task 6 approved" / "Approved Task 2"; approving Task 2 of one plan is not re-deriving Task 2 of another. Jaccard is confounded with decision-set size (the [.35,.50) band is 100% subagent-side at 1.05 decisions per side, while six-decision sessions cannot clear 0.15 however identical their reasoning), and the tokenizer strips no stopwords (stripping moves 152k pairs out of [.05,.15) and 6.9x more to zero, so most mid-band signal was function words). Wrote a revised spec (2026-08-04-stage-a-detector-calibration-design.md) that compares seven candidate feature designs rather than sweeping one threshold, and an 8-task plan. (2) Built the harness: 4 pure libs (features, fixed-allocation sampler, shared chat client, population-weighted scoring) + 3 CLIs (sample, label, calibrate), 202 tests, typecheck green across all three configs. (3) Sampled 303 pairs across 8 strata from a frozen 467,267-pair frame; every stratum population reconciled against the pre-registered spec (A1 53 vs 53, A2 843 vs 843, P1 2,066 vs 2,067, linked 82 vs 82). (4) Blind-judged all 303 with gemma-4-26b, 30/30 binary agreement with qwen3.6-35b, zero parse failures. (5) Swept 10,560 configurations, population-weighted with Kish effective sample sizes.

**Findings:** Shipped detector: precision 6.7% [1.2, 29.8], recall 0.1% [0, 72.5]; it fires 15 times and gets one right. NO configuration of the 10,560 reaches a 50% precision lower bound, so there is no defensible interrupt firing threshold on this feature space (direct problem for #428). The semantic leg did not rescue it: P2+P3, the 60 pairs at the top of the cosine distribution, produced zero genuine hits and cosine appears nowhere in the Pareto front. Best counting config is rawPooled >= 0.1, gap 30d, exclude subagents (19.8% / 81.9%), and excluding subagents is the single largest lever in the sweep. Independently confirmed the #427 denominator bug at full scale: 1,266,809 of 1,905,497 eligible pairs (66.5%) have a decisionless side and can never be positive.

**Decisions:** Stage A reports precision plus a base-rate finding, not calibration curves - six positives carry the whole result, so the readout is marked PROVISIONAL until the human spot check returns. Spot check retargeted from a random 10/10 split to all 6 GENUINE plus 14 from A4, because one missed positive in A4 (40 sampled of 256,228) moves the population estimate by ~6,400 while a wrong A1 label moves it by one. Corpus-derived artifacts (sample.jsonl, labels*.jsonl, spot-check.md) dropped from the ten unpushed commits via filter-branch and gitignored: they carry client names, venture names and internal hostnames, and this repo publishes publicly.

**Mistakes worth keeping:** The judge cache keyed on model + USER prompt only, so editing the rubric replayed 300/303 stale verdicts and returned a byte-identical distribution that read as convergence. The module docstring asserted the opposite behaviour, which is what made it invisible. Fixed by putting the system prompt in the key. Separately, the raw artifacts were committed without checking the remote first.

**State:** 11 commits on main, UNPUSHED pending a decision on whether the calibration report's 6-row GENUINE table (which names Hermes, NocoDB, NxtSites) is acceptable to publish. Tests 202/202 on scripts, typecheck green. Board: #426 In Progress and blocked on the spot check, #427 updated with the denominator finding.

**Next:** Edward's 20-pair spot check gates everything downstream. #427's denominator fix is independent of the labels and can land now. Stage C (#428) needs re-scoping rather than building, given no configuration supports an interrupt.

## 2026-08-04 - #393 readout, #394 hybrid fix landed, dead eval harnesses found and fixed, re-derivation pivot

**Changes:** (1) #393 readout ran: usefulness@pull 79.4% (n=80) vs 72.4% baseline, join rate 40%->81.6% (the real result -- U1's conversation_id work roughly doubled how much genuine-pull traffic is even scoreable), frequency 6.3 genuine pulls/active-day vs the 4.2 pre-ship baseline. Delta on usefulness itself is inside noise (z=1.03) -- reported as "held above target," not as a proven gain. Report: reports/usefulness/2026-08-03-393-readout.md. Ungated #394 (hybrid tiebreaker fix), which had been waiting on this window since 07-07. (2) #394 landed: `queryTokens` now reaches `finalize()` on the hybrid leg, not just keyword (commit 6da68a5). Re-ran the locked private-bench gold set against the unchanged 07-06 corpus snapshot: hybrid R@5 74.0->80.0, R@3 54.0->62.0, R@1 8.0->18.0, keyword arm byte-identical across runs. The 07-07 diagnosis had claimed the tiebreaker was the ENTIRE cause of the 14pp drag -- it was about 43% of it (6pp of 14); an 8pp gap remains, concentrated almost entirely in temporal queries (keyword 100% vs hybrid 62.5%, n=8). Filed #419. Report: reports/private-bench/2026-08-03-hybrid-tiebreaker.md. (3) DISCOVERED while trying to run the private-bench harness: it threw at runtime. `tsconfig.json` only includes `src/**`, so `scripts/` -- home to every eval/bench harness including the one behind the PUBLISHED LongMemEval numbers -- had zero typecheck coverage and M6's tenant-ID threading broke every one of them silently. Fixed: added tsconfig.scripts.json wired into `npm run typecheck` (which CI already gates on, closing the hole for any future signature change) before touching a single call site, then fixed the 45 errors it surfaced across 10 files (commits 88fe7ae, b1da2b8). 4 of those errors predated M6 entirely -- a required field never set since 2026-07-06, two test doubles missing port methods, one line of dead code with an invalid literal -- meaning the eval tooling had been quietly rotting for weeks before anyone noticed. (4) Market-positioning assessment requested, then redirected: Edward pushed back twice, first that distribution isn't the current concern, then that "undifferentiated in AI memory" was too strong a claim to accept at face value. Reframed Claude Code's own shipped memory (98 hand-provenanced files in this workspace) as a consumer of NLM's output, not a competitor to out-position. The actual answer was found in NLM's own tooling: the founding success criterion, "re-derivation rate trending downward," has never been measurable -- its denominator (every entity-sharing session pair) grew 67% in a month while the numerator moved 4 to 9, so the rate mechanically falls as the product gets used. Recomputed session-level: 0.36%, itself not believable given the detector's all-four-conditions-required, word-overlap-only design. Wrote docs/superpowers/specs/2026-08-03-re-derivation-detection-design.md (commit 7edce7c): calibrate against hand labels first (gates everything else), fix the metric, ship an in-flight interrupt, then reuse the causal-replay harness to measure it. NocoDB #426-429.

**Decisions:** Team NLM hosting batch (M5/M9/M10, #410) paused at P3 -- the tenancy substrate (M1-M4,M6,M7, all done) stays valuable for a future decision-ledger build but isn't the current bet; re-derivation detection is. Market-positioning and distribution work (#421 memory-promotion bridge, #422 buyer validation, #423 publish writeup, #424 ledger export, #425 reposition, #363 desktop .dmg) all demoted to P2/P3, re-derivation Stages A-D promoted to P1/P2. #423 re-scoped to wait for the Stage D causal result and publish it alongside the existing 0.881 causal-replay number rather than alone.

**State:** main pushed through 7edce7c. `npm run typecheck` green across src+tests+scripts (new coverage). Full suite 2333+/2568 passing, 7 pre-existing pg-connectivity failures only. Corpus 8,673 sessions; reprocess remainder (462 sessions, #390) still pending a quiet Studio window.

**Next:** Stage A (#426) -- hand-label a re-derivation ground-truth set and measure the detector's actual recall before touching thresholds or building anything on top of it. #419 (hybrid temporal gap) is the next recall-quality lead once Stage A frees up bandwidth. #416 (835 truncated session ids) is still the oldest open P1, untouched.

_Older entries archived in CHANGELOG-2026.md_
